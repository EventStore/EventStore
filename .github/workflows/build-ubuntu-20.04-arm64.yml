name: Build Ubuntu 20.04 ARM64
on:
  pull_request:
    paths-ignore:
      - "docs/**"
      - "samples/**"
      - "**.md"
  push:
    branches:
      - master
      - release/oss-v*
    tags:
      - oss-v*
    paths-ignore:
      - "docs/**"
      - "samples/**"
      - "**.md"

jobs:
  build:
    uses: EventStore/EventStore/.github/workflows/build-reusable.yml@d04a6dd08736276eb740fa9783b610b7242463e4
    with:
      os: ubuntu-20.04-arm64
      arch: arm64